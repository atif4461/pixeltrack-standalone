#ifndef HeterogeneousCore_CUDAUtilities_interface_HistoContainer_h
#define HeterogeneousCore_CUDAUtilities_interface_HistoContainer_h

#include <algorithm>
#include <numeric>
#include <atomic>
#include <cstddef>
#include <cstdint>
#include <type_traits>
#include <execution>
#include <ranges>

#include "CUDACore/AtomicPairCounter.h"
#include "CUDACore/cudaCheck.h"
#include "CUDACore/cuda_assert.h"

namespace cms {
  namespace cuda {

    template <typename Histo, typename T>
/**
 * Brief description of function to count from vector
 *
 * Detailed description of function to count from vector
 * 
 * @param h histogram object
 * @param nh number of histograms
 * @param v input vector
 * @param offsets offset values
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    void countFromVector(Histo *__restrict__ h,
                         uint32_t nh,
                         T const *__restrict__ v,
                         uint32_t const *__restrict__ offsets) {
      auto nt{offsets[nh]};
      auto iter_nt{std::views::iota(0U, nt)};
      std::for_each(std::execution::par, std::ranges::cbegin(iter_nt), std::ranges::cend(iter_nt), [=](const auto i) {
        auto off = std::upper_bound(offsets, offsets + nh + 1, i);
        assert((*off) > 0);
        int32_t ih = off - offsets - 1;
        assert(ih >= 0);
        assert(ih < int(nh));
        (*h).count(v[i], ih);
      });
    }

    template <typename Histo, typename T>
/**
 * @brief Fills a histogram from a vector in parallel execution
 * 
 * @param h Pointer to the histogram to be filled
 * @param nh Number of histograms
 * @param v Constant vector of values to fill the histogram
 * @param offsets Array of offsets for the histograms
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    void fillFromVector(Histo *__restrict__ h,
                        uint32_t nh,
                        T const *__restrict__ v,
                        uint32_t const *__restrict__ offsets) {
      auto nt{offsets[nh]};
      auto iter_nt{std::views::iota(0U, nt)};
      std::for_each(std::execution::par, std::ranges::cbegin(iter_nt), std::ranges::cend(iter_nt), [=](const auto i) {
        auto off = std::upper_bound(offsets, offsets + nh + 1, i);
        assert((*off) > 0);
        int32_t ih = off - offsets - 1;
        assert(ih >= 0);
        assert(ih < int(nh));
        (*h).fill(v[i], i, ih);
      });
    }

    template <typename Histo>
/**
 * Launches zero initialization for histogram object
 *
 * Initializes all offset values of the histogram to zero
 *
 * @param h pointer to the histogram object to be initialized
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    inline __attribute__((always_inline)) void launchZero(Histo *__restrict__ h) {
      uint32_t *poff = (uint32_t *)((char *)(h) + offsetof(Histo, off));
      int32_t size = offsetof(Histo, bins) - offsetof(Histo, off);
      assert(size >= int(sizeof(uint32_t) * Histo::totbins()));
      ::memset(poff, 0, size);
    }

    template <typename Histo>
    inline __attribute__((always_inline)) void launchFinalize(Histo *__restrict__ h) {
      h->finalize();
    }

    template <typename Histo, typename T>
/**
 * Fill many histograms from a vector of values.
 *
 * This function launches zero initialization of histograms, counts values from a vector,
 * finalizes the histogram and fills it from the vector.
 *
 * @param h         pointer to the histogram object
 * @param nh        number of histograms
 * @param v         array of values
 * @param offsets   array of offsets
 * @param totSize   total size
 * @param nthreads number of threads
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    inline __attribute__((always_inline)) void fillManyFromVector(Histo *__restrict__ h,
                                                                  uint32_t nh,
                                                                  T const *__restrict__ v,
                                                                  uint32_t const *__restrict__ offsets,
                                                                  uint32_t totSize,
                                                                  int nthreads) {
      launchZero(h);
      countFromVector(h, nh, v, offsets);
      h->finalize();
      fillFromVector(h, nh, v, offsets);
    }

    template <typename Assoc>
    void finalizeBulk(AtomicPairCounter const *apc, Assoc *__restrict__ assoc) {
      assoc->bulkFinalizeFill(*apc);
    }

    // iteratate over N bins left and right of the one containing "v"
    template <typename Hist, typename V, typename Func>
/**
 * Brief description: Applies a function to each bin in a histogram within a specified range.
 *
 * Detailed description: This function iterates over a subset of bins in a histogram,
 * starting from the bin that corresponds to a given value and applying a provided function
 * to each bin in the range. The range is defined by the number of neighboring bins to consider.
 *
 * Parameters:
 *   hist - The input histogram.
 *   value - The value used to determine the central bin.
 *   n - The number of neighboring bins to consider on either side of the central bin.
 *   func - The function to apply to each bin in the range.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    __forceinline__ void forEachInBins(Hist const &hist, V value, int n, Func func) {
      int bs = Hist::bin(value);
      int be = std::min(int(Hist::nbins() - 1), bs + n);
      bs = std::max(0, bs - n);
      assert(be >= bs);
      for (auto pj = hist.begin(bs); pj < hist.end(be); ++pj) {
        func(*pj);
      }
    }

    // iteratate over bins containing all values in window wmin, wmax
    template <typename Hist, typename V, typename Func>
/**
 * Brief description of function forEachInWindow
 *
 * Detailed description of function forEachInWindow
 * Applies a given function to all elements within a specified window of a histogram
 *
 * @param hist constant reference to the input histogram
 * @param wmin minimum value of the window
 * @param wmax maximum value of the window
 * @param func constant reference to the function to apply
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
    __forceinline__ void forEachInWindow(Hist const &hist, V wmin, V wmax, Func const &func) {
      auto bs = Hist::bin(wmin);
      auto be = Hist::bin(wmax);
      assert(be >= bs);
      for (auto pj = hist.begin(bs); pj < hist.end(be); ++pj) {
        func(*pj);
      }
    }

    template <typename T,                  // the type of the discretized input values
              uint32_t NBINS,              // number of bins
              uint32_t SIZE,               // max number of element
              uint32_t S = sizeof(T) * 8,  // number of significant bits in T
              typename I = uint32_t,  // type stored in the container (usually an index in a vector of the input values)
              uint32_t NHISTS = 1     // number of histos stored
              >
    class HistoContainer {
    public:
      using Counter = uint32_t;

      using CountersOnly = HistoContainer<T, NBINS, 0, S, I, NHISTS>;

      using index_type = I;
      using UT = typename std::make_unsigned<T>::type;

/**
 * Calculates the integer base 2 logarithm of a given unsigned 32 bit integer.
 *
 * @param v The input value.
 * @return The integer base 2 logarithm of the input value.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      static constexpr uint32_t ilog2(uint32_t v) {
        constexpr uint32_t b[] = {0x2, 0xC, 0xF0, 0xFF00, 0xFFFF0000};
        constexpr uint32_t s[] = {1, 2, 4, 8, 16};

        uint32_t r = 0;  // result of log2(v) will go here
        for (auto i = 4; i >= 0; i--)
          if (v & b[i]) {
            v >>= s[i];
            r |= s[i];
          }
        return r;
      }

      static constexpr uint32_t sizeT() { return S; }
      static constexpr uint32_t nbins() { return NBINS; }
      static constexpr uint32_t nhists() { return NHISTS; }
      static constexpr uint32_t totbins() { return NHISTS * NBINS + 1; }
      static constexpr uint32_t nbits() { return ilog2(NBINS - 1) + 1; }
      static constexpr uint32_t capacity() { return SIZE; }

      static constexpr auto histOff(uint32_t nh) { return NBINS * nh; }

/**
 * Returns the binary representation of the input value
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      static constexpr UT bin(T t) {
        constexpr uint32_t shift = sizeT() - nbits();
        constexpr uint32_t mask = (1 << nbits()) - 1;
        return (t >> shift) & mask;
      }

/**
 * Brief description of the function to reset values
 *
 * Detailed description of the function to reset values
 * Resets all elements to zero
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      void zero() {
        for (auto &i : off)
          i = 0;
      }

/**
 * Adds counters from another CountersOnly object to this object
 * @param co the CountersOnly object to add from
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      inline void add(CountersOnly const &co) {
        for (uint32_t i = 0; i < totbins(); ++i) {
          std::atomic_ref<Counter> off_atomic{off[i]};
          off_atomic.fetch_add(co.off[i]);
        }
      }

/**
 * Increments the counter at the specified bin index.
 *
 * @param b the bin index
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void countDirect(T b) {
        assert(b < nbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        ++off_atomic;
      }

/**
 * Brief description of function to decrement atomic counter and store index
 *
 * Detailed description of function to update bin data structure
 *
 * @param b  input parameter of type T
 * @param j  input parameter of type index_type
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void fillDirect(T b, index_type j) {
        assert(b < nbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        auto w = off_atomic--;
        assert(w > 0);
        bins[w - 1] = j;
      }

/**
 * Brief description of function to perform bulk filling operation
 *
 * Detailed description of function to add elements to AtomicPairCounter
 * and update offset and bin values accordingly
 *
 * @param apc reference to AtomicPairCounter object
 * @param v array of indices to be filled
 * @param n number of elements to fill
 * @return integer value representing result of bulk fill operation
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ int32_t bulkFill(AtomicPairCounter &apc, index_type const *v, uint32_t n) {
        auto c = apc.add(n);
        if (c.m >= nbins())
          return -int32_t(c.m);
        off[c.m] = c.n;
        for (uint32_t j = 0; j < n; ++j)
          bins[c.n + j] = v[j];
        return c.m;
      }

      __forceinline__ void bulkFinalize(AtomicPairCounter const &apc) { off[apc.get().m] = apc.get().n; }

/**
 * Brief description: Finalizes filling process for atomic pair counter.
 *
 * Detailed description: This function takes an AtomicPairCounter object as input,
 * retrieves its current state, and updates the offset array accordingly.
 * It checks for potential overflows and handles them by setting the last bin's value.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void bulkFinalizeFill(AtomicPairCounter const &apc) {
        auto m = apc.get().m;
        auto n = apc.get().n;
        if (m >= nbins()) {  // overflow!
          off[nbins()] = uint32_t(off[nbins() - 1]);
          return;
        }
        auto iter{std::views::iota(m, totbins())};
        auto off_ptr = off;
        std::for_each(std::execution::par, std::ranges::cbegin(iter), std::ranges::cend(iter), [=](const auto i) {
          off_ptr[i] = n;
        });
      }

/**
 * Brief description: Counts an occurrence of type T and updates the corresponding counter.
 *
 * Detailed description: This function takes an object of type T as input, determines its bin using the bin function, 
 * and then increments the atomic reference to the offset counter corresponding to that bin.
 *
 * Parameters:
 *   t - The object of type T to be counted.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void count(T t) {
        uint32_t b = bin(t);
        assert(b < nbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        ++off_atomic;
      }

/**
 * Fills a bin with an index value.
 *
 * @param t The type of data being filled into the bin.
 * @param j The index value to store in the bin.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void fill(T t, index_type j) {
        uint32_t b = bin(t);
        assert(b < nbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        auto w = off_atomic--;
        assert(w > 0);
        bins[w - 1] = j;
      }

/**
 * Brief description: Increments the counter for a specific bin.
 *
 * Detailed description: This function takes an object of type T and an unsigned 
 * 32-bit integer nh as input, calculates the corresponding bin, checks if it is within 
 * bounds, increments the offset, and then atomically increments the counter for that bin.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void count(T t, uint32_t nh) {
        uint32_t b = bin(t);
        assert(b < nbins());
        b += histOff(nh);
        assert(b < totbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        ++off_atomic;
      }

/**
 * Brief description: Decrements offset counter and stores index in corresponding bin.
 *
 * Detailed description: This function takes an object t, an index j, and an unsigned integer nh,
 * calculates the appropriate bin, checks bounds, decrements the offset counter atomically,
 * and stores the index in the calculated bin.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void fill(T t, index_type j, uint32_t nh) {
        uint32_t b = bin(t);
        assert(b < nbins());
        b += histOff(nh);
        assert(b < totbins());
        std::atomic_ref<Counter> off_atomic{off[b]};
        auto w = off_atomic--;
        assert(w > 0);
        bins[w - 1] = j;
      }

/**
 * Finalizes the offset calculation by performing an inclusive scan operation
 * in parallel execution mode to ensure accurate binning results.
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
      __forceinline__ void finalize() {
        assert(off[totbins() - 1] == 0);
        std::inclusive_scan(std::execution::par, off, off + totbins(), off);
        assert(off[totbins() - 1] == off[totbins() - 2]);
      }

      // Equivalent to finalize(), to be called withing device execution space as nested parallel algorithms are not supported
      __forceinline__ void finalizeSeq() {
        assert(off[totbins() - 1] == 0);
        std::inclusive_scan(off, off + totbins(), off);
        assert(off[totbins() - 1] == off[totbins() - 2]);
      }

      constexpr auto size() const { return uint32_t(off[totbins() - 1]); }
      constexpr auto size(uint32_t b) const { return off[b + 1] - off[b]; }

      constexpr index_type const *begin() const { return bins; }
      constexpr index_type const *end() const { return begin() + size(); }

      constexpr index_type const *begin(uint32_t b) const { return bins + off[b]; }
      constexpr index_type const *end(uint32_t b) const { return bins + off[b + 1]; }

      Counter off[totbins()];
      index_type bins[capacity()];
    };

    template <typename I,        // type stored in the container (usually an index in a vector of the input values)
              uint32_t MAXONES,  // max number of "ones"
              uint32_t MAXMANYS  // max number of "manys"
              >
    using OneToManyAssoc = HistoContainer<uint32_t, MAXONES, MAXMANYS, sizeof(uint32_t) * 8, I, 1>;

  }  // namespace cuda
}  // namespace cms

#endif  // HeterogeneousCore_CUDAUtilities_interface_HistoContainer_h
