#include <iostream>

#include "KokkosCore/kokkosConfigCommon.h"
#include "KokkosCore/kokkosConfig.h"


// The following comment is generated by an LLM. 
/**
 * @brief Main program entry point
 *
 * Initializes the Kokkos scope guard and prints a message to the console,
 * followed by executing a parallel for loop using Kokkos.
 *
 * @return Program exit status
 
 * @brief Parallel loop execution using Kokkos
 *
 * Executes a parallel for loop over a specified range using Kokkos,
 * printing each loop element index to the console.
 */ 
int main() {
  kokkos_common::InitializeScopeGuard kokkosGuard({KokkosBackend<KokkosExecSpace>::value});
  std::cout << "World" << std::endl;

  Kokkos::parallel_for(
      Kokkos::RangePolicy<KokkosExecSpace>(0, 4),
      KOKKOS_LAMBDA(const size_t i) { printf("Kokkos::parallel_for loop element %lu\n", i); });
  return 0;
}
