#ifndef PluginManager_h
#define PluginManager_h

#include <memory>
#include <mutex>
#include <string>
#include <unordered_map>

#include "SharedLibrary.h"

namespace edmplugin {
/**
 * @brief Manages the loading of plugins into the application
 * 
 * This class provides functionality for loading shared libraries as plugins
 * and managing their lifetime within the application It ensures that each
 * plugin is loaded only once and provides a way to retrieve a reference to
 * the loaded library
 * This comment was generated by meta-llama/Llama-3.3-70B-Instruct:None at temperature 0.2.
*/ 
  class PluginManager {
  public:
    PluginManager();

    SharedLibrary const& load(std::string const& pluginName);

  private:
    std::unordered_map<std::string, std::string> pluginToLibrary_;

    std::recursive_mutex mutex_;
    std::unordered_map<std::string, std::shared_ptr<SharedLibrary>> loadedPlugins_;
  };
}  // namespace edmplugin

#endif
